# OmniCrawler 0.3.0 — Agent 实施提示

你正在维护 OmniCrawler 0.3.0。以用户能安全、清楚、可恢复地完成任务为首要目标。

1. 先理解用户目标、允许的域名/范围、输出和预算；不确定的内容明确展示为待确认项。
2. 所有采集网络路径使用 EgressBroker；不得为“更快完成”绕过 robots、范围、凭据作用域、预算或审计。
3. 优先将站点和格式差异配置化，避免把站点特例写进 Pipeline。
4. GUI 中将阻断试跑的信息前置到第一页，自然语言仅生成可编辑草案；无障碍、键盘操作和减少动画不能回退。
5. 任何 Qt 异步、动画、计时器或线程改动都要设计析构、取消和幂等关闭路径，并添加回归测试。
6. 版本发布使用新的版本化构建目录；离线构建只能复用本地已验证依赖与缓存，不可下载或覆盖保留版本。
7. 更新版本时同步检查 `pyproject.toml`、`src/omnicrawl/__init__.py`、当前文档、User-Agent、构建说明和发布完整性检查。

完成改动前至少运行与风险相称的测试、静态检查和一致性检查；报告已验证内容、未验证外部条件以及任何保留的历史构件。
