# AGENTS.md — 给 AI / 自动化维护者的开发时指引

> 适用版本：**0.13.1** · 配置协议：v5 · 维护状态：现行
> 骨架来自 `docs/archive/OmniCrawler-0.5.0-Agent-Prompt.md` 的 7 条原则（0.5.0 时期），已按 0.13.1 升级。
> ★ 调用面（命令 / 参数 / 退出码 / stdout 形态）请看 [`docs/AGENT_GUIDE.md`](docs/AGENT_GUIDE.md)；
> 本文件是**开发时**的原则与纪律，两者不重复。

## 一、开发时原则（7 条）

1. **先理解目标与边界**：用户目标、允许的域名/范围、输出形态、预算；不确定的内容**显式展示为待确认项**，不替用户拍板。
2. **不得为「更快完成」绕过安全边界**：采集网络路径一律经 `EgressBroker`；robots、范围、凭据作用域、预算、审计都是硬边界。
3. **站点与格式差异配置化**：放进 source / template / fetcher / extractor，**不要把站点特例写进 Pipeline 编排层**。
4. **GUI 的可阻断信息前置**：首屏就要呈现；自然语言只生成**可编辑草案**；无障碍、键盘操作、减少动画不得回退。
5. **Qt 改动必配生命周期路径**：任何异步 / 动画 / 计时器 / 线程改动，都要设计析构、取消、幂等关闭，并补回归测试。
6. **发布用新的版本化构建目录**：离线构建只复用本地已验证的依赖与缓存，不下载、不覆盖保留版本。
7. **改版本要同步检查**：`pyproject.toml`、`src/omnicrawler/_version.py`、当前文档、User-Agent、构建说明、发布完整性检查。

## 二、本仓库的既有纪律（动手前先看）

**判据层级**：锚＝最终目标 / 现实条件 / 最终效果（**不可擅动**，要改需维护者同意）；
可动层＝执行原则、度量、门禁、baseline、豁免、兼容策略（**随优化演进**，判据是「是否仍服务锚」）。

**成效表述**：**禁用**工作量、文件行数、文件个数、测试数量、抽象层数量。
改用：用户影响 / 证据强度 / 发生场景 / 改动风险 / 维护成本；结构性工作用「定位与改动成本」。

**门禁**：
- 本地轻量自证：`python tools/self_verify.py --set static`（清单真源 `tools/gate_registry.py`）。
- 重活（三平台构建、便携冒烟、浏览器与 GUI 矩阵）**都在 CI**，本地不跑。
- 每批改动**本地提交**；红绿全绿再进下一批。

**判据纪律（最容易翻车）**：
- 判据必须**确定性**：「看起来正常」≠「运行时正确」；偶然正确 ≠ 正确。
- 新守卫**必须做反向断言**（装回缺口 ⇒ 必须红 ⇒ 字节级还原）；只绿不红等于没装护栏。
- 「选中 0 个用例 / 命令数为 0」必须判红 —— 空集合会让所有断言恒真。
- 行尾**按字节实测**（本仓主仓文件是 CRLF，市场仓是 LF）；注入式改动的锚点写 `\n` 会在 CRLF 文件上静默不匹配。
- **CI 看到的源码 ≠ 本机看到的源码**：跨仓守卫要留意 `constraints/market-ref.txt` 的固定快照。

**git 红线**：绝不用 `git rm`；绝不用 `git stash`；不在 worktree 里跑会触发 `git gc` 的操作；
远端真值只用 `gh api` 核实（本地 `origin/*` 可能被沙箱回滚而失真）。

**凭据**：冷私钥只在维护者指定的桌面档案目录（**不入仓、不读取其内容**，只按路径交给签名工具）；
绝不 echo / 复述任何 token。

## 三、已定决策（不要重新翻案）

- **不引入 MCP**：CLI 即 agent 面（`python tools/check_agent_surface.py --json` 导出契约）。
- **许可白名单＝方向 B（收紧）**：插件与插件依赖的许可白名单**不含 AGPL / GPL 系**。
- **不融合 stealth-browser-mcp**：其价值主张落在「规避风控 / 隐匿身份」，属明确不融合范畴。

## 四、改完要报告什么

已验证的内容、未验证的外部条件、以及任何保留的历史构件 —— 分开写清楚，不要把「什么都没变」当成绩。
