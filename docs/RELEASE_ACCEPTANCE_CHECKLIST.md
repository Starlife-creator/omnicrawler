# 统一实机验收清单（全部优化完成后的那一次）

> 适用版本：**0.14.0**（代码侧最后提交以实际 HEAD 为准）· 建立：**2026-09-25** · 执行人：**维护者**（starlife）
> 维护状态：现行（可复用；每轮候选发布照此走一遍，或只走受影响的条目）

## 这份清单解决什么

代码侧 / 治理侧的优化项**已全部闭环**（§十 P0–P2、§十一代码侧 8/8 + U5、§十二 P0–P3、W1–W7、R1–R5）。
**剩下且无法由自动化替代的，只有"真人上手"这一件事** —— 本清单把它一次收拢，避免分散在多处、来回找。

> **为什么要专门做一次、而不是逐批做**：项目纪律「**结论不继承给后续版本**」——验收放在**最后**、
> 证据才对得上**终态版本**（`优化方案.md` §十一「U4 已拆分」裁定）。逐批做会出现"验的是中间态、
> 但发布的是终态"的错位。

## 一、先决条件（开始前逐条打勾）

| # | 事项 | 命令 / 位置 | 打勾 |
|---|---|---|---|
| P1 | **记录终态标识**：提交号 + 系统/依赖版本 + 工作区路径 | `git -C OmniCrawler rev-parse --short HEAD` | ☐ |
| P2 | 起走查环境（固定样本 + 隔离工作区 + 能力清单） | `python tools/walkthrough_env.py` | ☐ |
| P3 | 确认 CI 在终态提交上全绿 | `gh run list --limit 5`（quality / end-to-end 均 success） | ☐ |
| P4 | 数据隔离按平台确认（Windows 覆盖 `LOCALAPPDATA`、POSIX 覆盖 `HOME`） | 脚本会直接打印对应命令 | ☐ |

> **没有 P1 的三样标识，跨轮次结论不可比**（`docs/MANUAL_WALKTHROUGH.md` 原话）。

## 二、A 组 · 首个任务旅程（8 步人工走查）

> **主清单在 `docs/MANUAL_WALKTHROUGH.md`**（含每步操作 / 应有现象 / 结果记录表模板）——
> 本节只做**收拢与勾稽**，明细照该文档执行，不重复维护。

| # | 环节 | 关键判定（原文档口径摘要） | 打勾 |
|---|---|---|---|
| 0 | 打开 | 首屏及时出现、无空白窗口、上次项目/配置被记住 | ☐ |
| 1 | 建任务 | 五分区顺序可完成；无效输入就地提示，不静默失败 | ☐ |
| 2 | 试跑 | 有进行中反馈；失败给**可行动**诊断与下一步 | ☐ |
| 3 | 试跑回填 | 全量按钮解锁；改范围/字段后**重新变灰并说明原因** | ☐ |
| 4 | 运行 | 可暂停/停止且**真停**（终态=「已取消」而非「错误」）；界面不冻结 | ☐ |
| 5 | 复核 | 行数与实际一致（**列表任务含翻页合计 8 条**）；证据可见 | ☐ |
| 6 | 导出 | 生成 Markdown 交付物且内容完整；失败明确提示 | ☐ |
| 7 | 附件/OCR | 附件能取到；PDF 给**来源位置 + 不确定性**；该复核的进复核队列 | ☐ |
| 8 | 异常态 | 三态（空/加载/错误）可区分；错误不伪装成"没数据" | ☐ |

**A 组整体打勾**：☐ 全部通过　☐ 带保留通过（附复现路径）　☐ 不通过（附复现路径）

## 三、B 组 · PDF/OCR 人工部分（验收账本标记为"未知"的项）

> 来源：`OmniCrawler/docs/TASK_ACCEPTANCE_LEDGER.md` 第 4 行「附件→PDF/OCR→复核（**人工部分**）」，
> 状态＝**未知（须人工走查）**。**这是账本里唯一按定义不可自动化的工作流。**

| # | 步骤 | 工具 / 位置 | 应有现象 | 打勾 |
|---|---|---|---|---|
| B1 | 起可复用走查包 | `python tools/walkthrough_env.py`（固定样本 + 隔离工作区） | 环境就绪、能力清单打印 | ☐ |
| B2 | 按 8 步清单走 PDF 流程 | `docs/MANUAL_WALKTHROUGH.md` 第 7 步 | 字段带页码/原文证据；不确定性可见 | ☐ |
| B3 | 复核队列人工补齐 | 复核队列 → `apply-review` 写入 `human_review` 值 | 补齐后**再导出可见** | ☐ |
| B4 | 样本期望值对照 | `tools/walkthrough_demo_site.py` 的 `EXPECTED` | 数字与文档一致（有单测锁住，不漂移） | ☐ |
| B5 | 结论登记 | `审查记录.md`「人工走查记录」一节（**§2.3**） | 判定 + 现象 + 复现路径 | ☐ |

**B 组整体打勾**：☐ 通过　☐ 带保留通过　☐ 不通过

## 四、C 组 · §十 P2 两个官方 view 插件的真实体验

> 来源：`优化方案.md` §10.13 P2 收口的「**剩余**：实机验收（两插件装上后的真实体验）」。

**前置事实（已核实）**：

- 默认市场源 = `https://raw.githubusercontent.com/Starlife-creator/OmniCrawler-market/main`（`core/config.py:210`）
- 两个插件**已在市场目录**：`plugins/issue-wishlist`、`plugins/community-guide`（市场仓）
- Web 发现页**已上线**：`https://starlife-creator.github.io/OmniCrawler-market/`（HTTP 200，`build_type=workflow`）

| # | 步骤 | 操作 | 应有现象 | 打勾 |
|---|---|---|---|---|
| C1 | 打开市场页 | GUI → 插件市场 | 目录正常拉取；**官方徽章**与**已审核徽章**语义可区分且不互斥（M5） | ☐ |
| C2 | 安装 `issue-wishlist` | 市场页点安装 | 安装即用、**不自动联网**；面板出现「需求清单」`rich_text` 说明 + 列表 | ☐ |
| C3 | 手动刷新需求清单 | 面板内刷新按钮 | 拉 GitHub 开放 Issues（`api.github.com`，**零凭据**）；PR 被过滤；失败在**面板内**提示不崩 | ☐ |
| C4 | 安装 `community-guide` | 市场页点安装 | 纯静态 `rich_text` 四个官方入口链接；**零网络**（permissions=[]） | ☐ |
| C5 | 外链确认 | 点社区引导里的外链 | **弹出确认框**；拒绝则不开；确认则系统浏览器打开（§10.9 #21 外链降级判据） | ☐ |
| C6 | 受限长文本组件（P2.1） | 观察两插件面板渲染 | 只渲染白名单段类型（heading/paragraph/bullet/link）；段数/字符上限生效；不执行脚本 | ☐ |
| C7 | 卸载 / 停用 | 各自独立停机卸载 | 两个插件**互相独立**（卸载一个不影响另一个）；页签上没装的那项**不亮** | ☐ |
| C8 | 更新检查 | 每次启动的更新检查 | 有新版提示；无新版静默；不阻塞主流程 | ☐ |

**C 组整体打勾**：☐ 通过　☐ 带保留通过　☐ 不通过

## 五、D 组 · §十一 U 线登录会话 + QML（实机验证移出批序的部分）

> 来源：`优化方案.md` §11.5 / §11.7 / U4-验证（★ 2026-09-22 裁定：**移出批序**，并入本清单）。

| # | 步骤 | 操作 | 应有现象 | 打勾 |
|---|---|---|---|---|
| D1 | 真实站点登录 | 任务运行命中 401 / 302→login 时联动提示 → 一键跳转登录页并**预填账户**（U4） | 登录窗口为 **headed Playwright**；15 分钟可关可延长 | ☐ |
| D2 | 会话保存 | 登录成功后保存会话 | 快照落盘（**U5：AES-GCM 加密信封**，`8e64087`）；**落盘内容不含 cookie 明文子串** | ☐ |
| D3 | 免登录复跑 | 重跑同一任务 | **无需再次登录**即跑通（双引擎复用同一会话） | ☐ |
| D4 | 旧明文快照迁移 | 若存在历史明文快照 | 首读静默升级为加密信封；**明文残留 = 0**；cookie 语义逐字段相等 | ☐ |
| D5 | 损坏信封 | 人为损坏快照文件 | **显式报错**并引导重新登录（不静默失败、不假装成功） | ☐ |
| D6 | 密钥丢失场景 | 删热密钥后启动 | 会话全部作废 ⇒ **重新登录即恢复**（不建恢复通道，见 §11.8.3 裁定） | ☐ |
| D7 | QML 冻结包降级 | 用**冻结包**（非源码）启动 | 橱窗页**显式降级提示**（QML 不打包，`1474233`）；**体积核验**符合预期 | ☐ |
| D8 | 代理不同源实验 | 按 `审查记录.md` §9.4 #2 | 记录实际现象（用于判断是否需要 U6） | ☐ |

**D 组整体打勾**：☐ 通过　☐ 带保留通过　☐ 不通过

## 六、E 组 · U6 触发判定（★ 只做判定，不预设要做）

> **U6 不是待办，是"视实测启动"**（`优化方案.md` §11.8.2「U6-4」）。
> 本组的**唯一目的**：用真实站点确认触发条件是否成立。

| # | 判定条件 | 观测方法 | 结论 | 打勾 |
|---|---|---|---|---|
| E1 | 是否出现「**storage_state cookie 齐全，但请求仍 401**」的实测任务 | 在 D3/D8 中留意；或用 API 型任务实测 | 是 ⇒ **启动 U6**；否 ⇒ **只交 U5、U6 继续挂起** | ☐ |

**E 组结论**：☐ 触发（需启动 U6）　☐ 未触发（U6 保持挂起）

## 七、F 组 · 不属本次验收（只登记，避免混淆）

| 项 | 为何不在此列 |
|---|---|
| `TASK_ACCEPTANCE_LEDGER.md` 的 `适用版本` 从 0.12.0 升到当前版本 | 该账本每行都是**验收声明**，**不能靠改字升级**，要升必须**重跑**全部六条工作流的自动证据（B 组只重跑人工那条） |
| 附件→PDF/OCR 的**自动部分** | 已有端到端自动证据（账本「已验证（自动部分）」），不需人工 |
| §6.3 暂缓/明确不做各项 | 均为**有证据的主动不做**（附推翻条件），非欠债 |
| 远程 `<img>` 拉取 | 只是「**未复现**」不是「不可能」⇒ 需 Qt 版本/真实平台复测；**本清单不改判**，如需可加为观察项 |

## 八、收口规则

1. **每个"带保留通过 / 不通过"必须附复现路径**（原始文档要求，本清单沿用）。
2. 走查发现的每个问题按两类处理（沿用 `MANUAL_WALKTHROUGH.md`）：
   - **可自动化的** → 回补测试（`tests/integration/test_first_task_journey.py` 或对应视图测试）；
   - **纯体验/视觉问题** → 记入 `优化方案.md` §四.5，按「用户影响 / 发生场景 / 改动风险」排序。
3. **结论落点**：`审查记录.md`「人工走查记录」一节（**§2.3**，2026-09-20 起该节由模板改为登记表）追加本轮结论。
4. 全部组完成后：**若 E 组触发** ⇒ 走「U6 方案 → 风险评估 → 实施」独立批次；
   **若未触发** ⇒ §十 P2 与 §十一 的实机验收**到此闭环**，可进入发布流程（`发布与维护流程.md` §A.8）。

---

**引用约定**：能力/操作明细引 `docs/MANUAL_WALKTHROUGH.md`；行动项与批次引 `优化方案.md` §十 / §十一；
验收声明真源引 `docs/TASK_ACCEPTANCE_LEDGER.md`（当前 `适用版本：0.12.0`，本条清单完成后按 F 组规则整体重跑再升级）；
本清单只做**收拢与勾稽**，不复制明细。
