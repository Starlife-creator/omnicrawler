# 2.0 SDK 与插件兼容政策

公共入口仅为 `omnicrawl.sdk` 及文档标注的 plugin-sdk。stable 接口按语义化版本维护，删除前至少一个小版本弃用期；preview 可在小版本演进但必须给迁移说明；internal 不构成兼容承诺。

插件必须声明 ID、版本、发布者、核心兼容范围、权限和签名。权限变化需重新批准；未知权限、撤回版本、无签名或契约测试失败默认拒绝。插件仅通过受限 IPC 获取记录、附件、临时目录和经统一出口控制的网络能力。
