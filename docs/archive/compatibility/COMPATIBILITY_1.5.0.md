# 1.5.0 兼容与迁移说明

- 配置协议继续为 v5；1.0/1.1/1.4 配置按既有迁移链读取。
- 首页和简单模式仅修改明确呈现的字段；`passthrough` 中的高级与未知字段原样保留，并显示摘要。
- 五步向导顺序、YAML入口、菜单、快捷键、问号入口、CLI 和 Worker 协议保持兼容。
- 帮助按钮参数从自由文本迁移为稳定 `help_id`；这是 GUI 内部接口变化，不影响项目配置。
- 自然语言助手仅生成草案，任何正式运行仍需试跑和用户确认。

回滚到 1.4.0 前先复制工作区；本版本未提高配置协议版本，已保存任务可由 1.4.0 读取，但首页偏好与无障碍偏好会被旧版忽略。
