# 1.6.0 兼容说明

- 配置协议仍为 v5；桌面三模式继续共享同一配置与 IR。
- SDK 标记为 `1.0-preview`；只有 `API_STABILITY` 中 stable 名称获得正式弃用期。
- 旧进程内插件仍可按既有设置运行，但开发者工具会建议迁移至签名清单和隔离 Runner；2.0 前不会静默删除旧插件配置。
- 1.5 工作区和复核文件无需迁移。回滚前复制工作区；1.5 会忽略新的 SDK/插件开发资产。
