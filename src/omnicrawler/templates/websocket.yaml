template_version: 1
project: {name: 示例项目-WebSocket, workspace: work/websocket}
source:
  kind: websocket
  seeds: [wss://stream.example.invalid/ws]
  subscribe: {type: subscribe, channel: notices}
  max_messages: 100
  duration_seconds: 60
crawl: {max_pages: 100, same_host: true}
http: {user_agent: "OmniCrawler/0.12.0 (+contact: crawler@example.org)", allow_private_network: false}
